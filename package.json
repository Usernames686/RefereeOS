{
  "name": "refereeos",
  "private": true,
  "version": "0.1.0",
  "description": "Award-winning multi-agent preprint triage and reproducibility assistant.",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint"
  },
  "engines": {
    "node": ">=20"
  }
}
